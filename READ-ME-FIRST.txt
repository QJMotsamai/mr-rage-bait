MR RAGE BAIT — v15   MODEL DEFAULT 2.5 -> 3.5 FLASH-LITE

TWO FILES.  server.js  and  lib/ai.js

DO THIS SECOND. The urgent fix is the Render variable:

    GEMINI_MODEL = gemini-3.5-flash-lite

That alone unbreaks your chat in about 3 minutes, no upload.

This zip only changes the built-in DEFAULT from the retired
2.5-flash-lite to 3.5-flash-lite, so that if the env var is ever
deleted the app still works instead of breaking.

It also makes a retired model fail with a plain sentence:
"The owner is pointing me at a model that no longer exists."
instead of a raw Google error.

UPLOAD
  1. Repo home -> Add file -> Upload files -> server.js -> Commit
  2. Click lib -> Add file -> Upload files -> ai.js    -> Commit
